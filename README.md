```markdown
# Heart Risk Prediction

Прогнозирование риска сердечного приступа на основе клинических, антропометрических и поведенческих данных пациента.

## 📁 Структура проекта

```
project-heart-risk-prediction/
├── data/                          # Исходные данные (не коммитятся)
│   ├── heart_train.csv            # Обучающий датасет (с таргетом)
│   └── heart_test.csv             # Тестовый датасет (без таргета)
│
├── notebooks/                     # Jupyter Notebook с EDA и обучением модели
│   └── 01_eda_and_modeling.ipynb
│
├── src/                           # Основной код приложения (OOP)
│   ├── app/                       # FastAPI-сервер
│   │   └── main.py                # Эндпоинты /predict и /health
│   └── core/                      # Ядро ML-логики
│       ├── data_processor.py      # Класс DataProcessor
│       └── model_wrapper.py       # Класс ModelWrapper
│
├── tests/                         # Тесты API
│   └── test_api.py
│
├── docs/                          # Документация
│   ├── usage.md                   # Инструкция по запуску и использованию
│   └── classes.md                 # Описание архитектуры и классов
│
├── model.pkl                      # Сохранённая модель CatBoost
├── label_encoder.pkl              # Сохранённый LabelEncoder для колонки Gender
├── submission.csv                 # Итоговые предсказания для тестового датасета
└── requirements.txt               # Зависимости Python
```

## 🚀 Как запустить

1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Запустите сервер:
   ```bash
   uvicorn src.app.main:app --host 127.0.0.1 --port 8001 --reload
   ```

3. Откройте документацию API:
   ```
   http://127.0.0.1:8001/docs
   ```

4. Отправьте POST-запрос на `/predict` с CSV-файлом — получите JSON с предсказаниями.

## 📤 Формат предсказаний

Сервис возвращает JSON вида:
```json
{
  "predictions": [
    {"id": 123, "prediction": 0},
    {"id": 456, "prediction": 1},
    ...
  ]
}
```

Итоговый файл `submission.csv` имеет формат:
```csv
id,prediction
123,0
456,1
...
```

## 📚 Документация

- Подробная инструкция по использованию: [`docs/usage.md`](docs/usage.md)
- Описание архитектуры и классов: [`docs/classes.md`](docs/classes.md)

## 🛠 Технологии

- **Модель**: CatBoost
- **Backend**: FastAPI
- **Препроцессинг**: pandas, sklearn.preprocessing.LabelEncoder
- **Сохранение моделей**: joblib
```

Этот `README.md`:
- понятен любому пользователю,
- описывает структуру,
- даёт инструкцию по запуску,
- ссылается на остальную документацию,
- соответствует требованиям задания.

Можешь сразу скопировать его в корень проекта.